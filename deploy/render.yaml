services:
  - type: web
    name: surebet-pro
    env: python
    buildCommand: pip install flask gunicorn
    startCommand: gunicorn wsgi:application --bind 0.0.0.0:10000 --workers 1 --threads 4
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
